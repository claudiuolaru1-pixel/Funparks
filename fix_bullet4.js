const fs=require('fs');let c=fs.readFileSync('lib/screens/park_detail_screen.dart','utf8');const before=c.indexOf('Adult $a');const after=c.indexOf('Child $c ${park');if(before>0&&after>0){const bad=c.substring(before+8,after);c=c.replace('Adult $a'+bad+'Child','Adult $a \u2022 Child');}fs.writeFileSync('lib/screens/park_detail_screen.dart',c,'utf8');console.log('Bullet:',c.includes('\u2022')?'YES':'NO');